def encrypt_message(message, password, private_key, otp):
    # This is just a mock encryption function
    return f"ENCRYPTED({message}) WITH ({password}, {private_key}, {otp})"

def decrypt_message(encrypted_data, private_key, otp):
    # This is just a mock decryption function
    return "SIMULATED_ORIGINAL_MESSAGE"
